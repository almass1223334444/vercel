document.getElementById('giftBox').addEventListener('click', function() {
    var giftImage = document.getElementById('giftImage');
    
    // Toggle the 'hidden' class to show/hide the image
    giftImage.classList.toggle('hidden');
    
    // You can add more animation or loading logic here if needed
});
